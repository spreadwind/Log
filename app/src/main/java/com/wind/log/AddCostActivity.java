package com.wind.log;

/*
public class AddCostActivity extends AppCompatActivity implements View.OnClickListener {
    private EditText mEditTextMoney;
//    private EditText mEditTextRemarks;
    private EditText mEditTextTime;
    private Spinner mSpinerType;
    private Button mButtonAdd;
    private Button mButtonCancel;
    private DatePicker mCostDate;

    private static final String[] types = {"衣服", "饮食", "房租水电", "交通", "娱乐", "生活用品", "其他"};

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_cost);

        mEditTextMoney = (EditText) this.findViewById(R.id.edt_add_cost_money);
        mCostDate = (DatePicker) this.findViewById(R.id.dp_add_cost_time);
        mSpinerType = (Spinner) this.findViewById(R.id.sp_cost_type);
        mButtonAdd = (Button) findViewById(R.id.btn_save);
        mButtonCancel = (Button) findViewById(R.id.btn_cancel);

    }

}
*/
